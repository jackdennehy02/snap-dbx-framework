source_object: cdc_material

# ── Target ──────────────────────────────────────────
object_name: processed_material
enabled: true
catalog: dev_setl
schema: 02_silver
comment:                                 # Unity Catalog table COMMENT

# ── Read / Write Behaviour ───────────────────────────
write_mode: materialized_view            # materialized_view (required when source is a CDC table — APPLY CHANGES INTO uses MERGE, which streaming tables cannot read) | streaming_table (only valid if sourcing from an append-only table)
read_mode: snapshot                      # snapshot = full table scan — required when write_mode is materialized_view | stream = incremental read (streaming_table only)

# ── Keys ─────────────────────────────────────────────
primary_key_columns:                     # populated from objects.yml
  - material_key

# ── Column Ordering ──────────────────────────────────
leading_columns:                         # optional — columns to place first; everything else follows in source order
  # - col_a
  # - col_b

# ── Columns ──────────────────────────────────────────
# Only define columns that need transformation. Everything else passes through automatically.
# Framework audit columns (__START_AT, __END_AT, __etl_is_current etc.) are handled by the framework.
columns:

  # - source_col: raw_col
  #   target_col: col
  #   data_type: DECIMAL(10,2)
  #   expression: "CAST(raw_col AS DECIMAL(10,2))"